language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
before_install:
    - sudo apt-get update -qq
install:
    - pip install .
    - pip install nose-cov
script: nosetests -sv --with-coverage --cover-package=twitch twitch
after_success:
    - pip install coveralls
    - coveralls
